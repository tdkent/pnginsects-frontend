const getImages = async () => {};

const Gallery = async () => {
  // const images = await getImages();
  // console.log("🚀 ~ file: Gallery.tsx:8 ~ Gallery ~ images:", images);
  return <div>Gallery</div>;
};

export default Gallery;
